#  switches!
  - platform: command_line
    switches:
      # turns off retropie
      retropie:
        command_off: 'ssh retropie sudo shutdown now'
        command_state: 'ping retropie -c1 ; echo $?'
        value_template: “{{ is_state(‘binary_sensor.retropie’, ‘on’) }}”
